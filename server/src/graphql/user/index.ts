import { queries } from "./queries";
import { mutations } from "./mutation";
import { resolvers } from "./resolver";
import { typeDefs } from "./typedefs";

export const Userinfo = { queries, mutations, resolvers, typeDefs }